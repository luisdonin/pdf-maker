{
  "name": "pdf-maker",
  "version": "1.0.0",
  "description": "A web-based PDF form builder that allows users to upload PDFs and add interactive form fields",
  "main": "index.html",
  "scripts": {
    "start": "npx http-server -p 8080",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["pdf", "forms", "form-builder", "pdf-lib"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^3.11.174"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
